# تقرير المرحلة 0 — التحضير

**التاريخ:** 3 مارس 2026  
**الحالة:** تم تنفيذ المهام الآلية؛ مطلوب إجراءات منك (مفاتيح API، واختيارياً Python وواجهة Forge).

---

## ما تم إنجازه

| # | المهمة | النتيجة |
|---|--------|---------|
| 0.1 | التحقق من البيئة | ✅ **Node.js** v24.12.0 — ✅ **npm** 11.6.2 — ⚠️ **Python** غير موجود أو غير مضاف إلى PATH |
| 0.2 | إنشاء مجلدات العمل | ✅ تم إنشاء `projects` و `jobs` داخل المجلد الحالي `b11` |
| 0.3 | تجهيز قالب مفاتيح API | ✅ تم إنشاء `.env.example` و `.gitignore` (لا يرفع .env أبداً) |
| 0.4 | واجهة Forge v5.4.1 | ⚠️ غير موجودة في هذا المشروع — إن كانت في مكان آخر، تأكد أنها ترسل POST للخادم عند الربط |

---

## مسارات المجلدات (على Windows)

الوثيقة الأصلية تستخدم مسارات Linux (`/home/user/...`). هنا تم التكيف للمجلد الحالي:

| الغرض | المسار |
|--------|--------|
| مشروع الخادم (سيُنشأ في المرحلة 1) | `c:\Users\b.alsalman\b11\projects\` |
| مجلد مهام التوليد | `c:\Users\b.alsalman\b11\jobs\` |

عند تنفيذ المرحلة 1 سننشئ `badr-forge-server` داخل `projects` وسنعدّل مسار `JOBS_DIR` في الكود ليشير إلى `c:\Users\b.alsalman\b11\jobs` (أو مسار مطلق مناسب).

---

## مطلوب منك قبل المرحلة 1

1. **مفتاح Claude (Anthropic)**  
   - احصل على مفتاح من [console.anthropic.com](https://console.anthropic.com).  
   - انسخ `.env.example` إلى `.env.local` (داخل مشروع الخادم لاحقاً) وضع القيمة:  
     `CLAUDE_API_KEY=sk-ant-xxxxx`

2. **Python (للمرحلة 3 — Aider)**  
   - إذا كنت ستنفذ على **Kwork Cloud Desktop (Linux)** لاحقاً، يمكن تثبيت Python هناك.  
   - على **Windows** الآن: يمكن تثبيت Python من [python.org](https://www.python.org/downloads/) وإضافته إلى PATH، أو تأجيل ذلك حتى المرحلة 3.

3. **واجهة Forge**  
   - إن كانت واجهة v5.4.1 في مشروع آخر أو على سيرفر، تأكد أنها جاهزة لترسل طلبات إلى `POST /api/forge/run` عند الربط لاحقاً.

---

## جاهزية المرحلة 0

- [x] بيئة Node/npm جاهزة
- [x] مجلدات `projects` و `jobs` موجودة
- [x] قالب `.env.example` و `.gitignore` جاهزان
- [ ] **أنت:** إعداد مفتاح API في `.env.local` عند إنشاء مشروع الخادم (المرحلة 1)
- [ ] **اختياري الآن:** تثبيت Python إذا أردت تشغيل Aider محلياً لاحقاً
- [ ] **اختياري الآن:** التأكد من واجهة Forge إن كانت منفصلة

---

**الخطوة التالية:** البدء بالمرحلة 1 — إنشاء مشروع Next.js للخادم الوسيط ومسارات API.
